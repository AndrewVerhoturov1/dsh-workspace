# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260926T092440Z_8244
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: f933b8fae63c5028f1b3d0fc8783d8c7f700f7e8

## User intent

Проведи независимое актуальное исследование (текст, без ZIP/правок) выбора capability-only CodeRuntime для Postman Leader/Worker/Bridge PTC в DSH 0.1.1-rc.2. Основной кандидат QuickJS/WASM в Node Worker, альтернативы SES+Worker и Deno restricted process. По возможности опирайся на первичные документы/исходники quickjs-emscripten, DSH CodeRuntime, Node/Deno/SES и приведи кликабельные URL с конкретными утверждениями; старые chatgpt-content-reference не служат источниками. Оцени реально: async host bindings через QuickJS Promise и pending jobs, Promise.all concurrency, TS stripping, JSON clone/number limits, error semantics, console/output limit, cancellation/deadline/infinite loop/memory limit, dynamic import/eval escapes, WASM/QuickJS CVE/upgrades, worker termination, лицензии/deployment Windows, ограничения WASM VM и host callbacks. Важна безопасность НЕ только PTC SDK, но и точная executable boundary. Учитывай, что текущая версия presentation принимает mode:'code' не 'ptc', Leader allowlist 18 включая postman_worker_interrupt. Нужна взвешенная рекомендация с альтернативой на случай неудачного PoC, измеримыми acceptance tests и вопросами, которые требуют локального исходника. Можно ли выбрать QuickJS сейчас или сперва сравнить микропрототипы? Сокращай гипотезы, чётко различай подтверждённое документом и предположения. Ответ на русском.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260926T092440Z_8244>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260926T092440Z_8244>>>
