# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260922T233602Z_7885
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: eeb5f33890453188624bc38b18f0aa4fab2d89e9

## User intent

Исследуй на текущих dsh-workspace/upstream master безопасную capability composition будущего read-only Worker: filesystem read/glob/grep, web_search/web_fetch, browser QA и preview без shell. Inline до 3800 символов, paths/symbols/tests/SHA. Сравни Playwright MCP, Chrome DevTools MCP, Stagehand native и узкий host-owned facade: scope registrations, возможность скрыть raw catalog, ToolRestriction interaction, process/network/security risks. Для preview предложи v1 host-owned tool: static root policy, bind host, случайный порт, URL, health, lifetime, stop, QA-only против persistent user preview, ownership после one-shot disposal. Проверь совместимость удаления web tools и, возможно, read/glob/grep у Leader с текущими postman-leader instructions/runtime/tests; уточни минимальный Leader surface. Раздели факт/вывод/архитектурную рекомендацию/неизвестное. Не реализуй.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260922T233602Z_7885>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260922T233602Z_7885>>>
