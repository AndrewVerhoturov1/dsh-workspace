# Request Identity

REQ идентифицирует одну задачу.

Формат:
REQ_YYYYMMDDTHHMMSSZ_NNNN

Инициатор создаёт ID.
Runtime использует его для корреляции.
Артефакты используют этот ключ в именах.

REQ не сохраняется как пользовательская память.
