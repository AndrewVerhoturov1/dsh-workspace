# Проверка восстановления DSH — 11 сентября 2026

## Состояние на 10:48–10:50 (+05:00)

- Текущая система — Windows 10 Pro 22H2, сборка 19045.
- Активный образ: `E:\NativeBootWin10\Windows10Clean.vhdx`, 139590631424 байт. Он подключён как загрузочный виртуальный диск `Msft Virtual Disk` (Disk 3); переносить или заменять его нельзя.
- Резервная копия: `D:\Windows10Clean.vhdx`, 37987811328 байт. Файл существует и открывается для чтения.
- Свободно: C: около 69.0 GiB, D: около 145.6 GiB, E: около 169.9 GiB.
- Признаки обязательной перезагрузки CBS/WU отсутствуют; `PendingFileRenameOperations` присутствует, поэтому после установки потребуется повторная проверка.
- Kaspersky 21.26.4.406 запущен, служба AVP21.26 работает автоматически.
- Ashampoo WinOptimizer Pro 29 установлен; во время этой проверки его очистка и оптимизация не запускались.

## Резервная копия конфигурации

Создан каталог `D:\DSH-recovery-backup-20260911`.

- Сохранены только настройки профилей DSH, временные auth-настройки, workspace-файлы и файл состояния рынка.
- Файлы проекта DeepSeek Harness, `package.json`, `pnpm-lock.yaml` и `node_modules` проекта не изменялись.
- Секреты не раскрывались.
- SHA-256 и список файлов: `D:\DSH-recovery-backup-20260911\SHA256-MANIFEST.txt`.
- Для резервной копии применён защищённый ACL без доступа Everyone, Users и Authenticated Users; фактический доступ оставлен Andrew, SYSTEM и Administrators.

## Точка восстановления

Штатная попытка через встроенный Windows PowerShell завершилась отказом в доступе. Текущий процесс не повышен (`IsAdmin=False`). UAC не обходился, точка восстановления этим запуском не создана.

## Проверенные установщики и попытка установки

- `D:\DSH-recovery-installers-20260911\node-v24.21.0-x64.msi`
  - источник: `nodejs.org`, официальный Node.js v24.21.0 x64;
  - размер: 33230848 байт;
  - SHA-256: `bb0eaee134f9357f22aea915ee793343e627aefc1e66488164bac6915bce2cac`;
  - цифровая подпись: Valid, OpenJS Foundation.
- `D:\DSH-recovery-installers-20260911\Git-2.55.0.5-64-bit.exe`
  - источник: официальный релиз `git-for-windows/git` v2.55.0.windows.5;
  - размер: 65343712 байт;
  - SHA-256: `d065a4e23c3d9a6b5073d609b5be0830227ec3ca053c083ba385061ddfaf94c6`;
  - цифровая подпись: Valid, Johannes Schindelin.

Оба установщика совпали с опубликованными официальными SHA-256.

11.09.2026 была предпринята попытка запустить Node.js MSI через штатное повышение прав (`msiexec`, стандартная тихая установка для всех пользователей, без перезагрузки). После ожидания процесса подтверждения/завершения в системе не найдено ни процесса `msiexec`, ни записи установщика, ни файлов `C:\Program Files\nodejs\node.exe`, `npm.cmd`, `npx.cmd` и `corepack.cmd`; в новом чистом процессе команды `node`, `npm`, `npx`, `corepack` не обнаружены. Код завершения установщика не был возвращён средой запуска, поэтому результат считается не подтверждённым и установка остановлена. Git и pnpm после этого не запускались.

## Блокер продолжения

Текущий Codex-процесс не имеет прав администратора. Для продолжения нужен интерактивный запуск Node.js MSI с видимым подтверждением UAC; текущая попытка не дала подтверждённого результата. До явного успешного результата Node.js Git и pnpm не запускаются.

## Повторная проверка в новом процессе — 11.09.2026

- В официальном системном расположении Node.js (`C:\Program Files\nodejs` и `C:\Program Files (x86)\nodejs`) `node.exe` отсутствует; записей Node.js в системных и пользовательских разделах удаления программ не найдено.
- Найден только встроенный Node.js среды Codex: `C:\Users\Andrew\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`, версия `v24.19.0`, архитектура `x64`, платформа `win32`. Его цифровая подпись проверена: `Valid`, OpenJS Foundation. Это не подтверждает установку официального системного Node.js для DSH.
- В новом чистом процессе `npm` и `npx` как команды не найдены. PATH содержит путь только к встроенному Node.js среды Codex; профиль `C:\Users\Andrew\.dsh\profiles\web` и `node_modules` не читались и не изменялись.
- Проверенный установщик `D:\DSH-recovery-installers-20260911\node-v24.21.0-x64.msi` существует; подпись `Valid`, OpenJS Foundation; блокировка зоны отсутствует. Установщик Git также имеет `Valid` подпись, Johannes Schindelin.
- Kaspersky Service 21.26 запущен. Журнал `Kaspersky Event Log` включён, записей нет; зафиксированных событий блокировки Node.js MSI не обнаружено. Это означает отсутствие обнаруженных признаков блокировки, но не заменяет подтверждение установки.
- Проводник открыт с выделенным проверенным MSI Node.js: `D:\DSH-recovery-installers-20260911\node-v24.21.0-x64.msi`. Установка не запускалась.

## Текущий результат

`BLOCKED`: официальный системный Node.js 24 x64 не установлен. Следующее единственное действие пользователя: запустить выделенный MSI Node.js в Проводнике и подтвердить UAC/установку; после этого запросить повторную проверку.

## Повторная проверка после установки Node.js — 11.09.2026

- В новом чистом процессе подтверждены системные файлы `C:\Program Files\nodejs\node.exe`, `npm.cmd`, `npx.cmd` и `corepack.cmd`.
- Прямой запуск системных файлов дал: Node.js `v24.21.0`, архитектура `x64`, `win32`; npm `11.19.0`; npx `11.19.0`. Запись Node.js в удалении программ: версия `24.21.0`, издатель `Node.js Foundation`, дата установки `20260911`.
- Цифровая подпись `C:\Program Files\nodejs\node.exe`: `Valid`, OpenJS Foundation. Системный PATH содержит `C:\Program Files\nodejs\`; текущий процесс Codex сохранил старый PATH и поэтому по имени видит только встроенный runtime Codex. Профиль `C:\Users\Andrew\.dsh\profiles\web` и `node_modules` не изменялись.
- Записей Node.js MSI в журнале установщика за последние два часа не найдено; Kaspersky Service 21.26 запущен, журнал `Kaspersky Event Log` включён и пуст, признаков блокировки установки не обнаружено.
- Системный Git отсутствует: `C:\Program Files\Git\cmd\git.exe` и `C:\Program Files\Git\bin\git.exe` отсутствуют, записей Git в удалении программ нет. Обнаруженный `git 2.53.0` относится только к встроенной среде Codex.
- Проводник открыт с выделенным проверенным установщиком Git: `D:\DSH-recovery-installers-20260911\Git-2.55.0.5-64-bit.exe`. Установка не запускалась.

## Текущий результат

`BLOCKED`: Node.js 24 x64 установлен и подтверждён; системный Git отсутствует. Следующее единственное действие пользователя: запустить выделенный установщик Git и подтвердить UAC/установку; после этого запросить повторную проверку.

## Проверка Git и установка pnpm — 11.09.2026

- В новом чистом процессе подтверждён системный Git: `C:\Program Files\Git\cmd\git.exe`, версия `2.55.0.windows.5`. Основной `git.exe`, а также `C:\Program Files\Git\bin\git.exe` и `C:\Program Files\Git\mingw64\bin\git.exe` имеют действительную подпись Johannes Schindelin. Встроенный Git Codex не использовался.
- Системный Node.js по-прежнему доступен напрямую из `C:\Program Files\nodejs`: `v24.21.0`, `x64`; npm `11.19.0`, npx `11.19.0`. Подпись Node.js действительна, OpenJS Foundation.
- Системный PATH содержит `C:\Program Files\nodejs\` и `C:\Program Files\Git\cmd`; пользовательский PATH содержит `C:\Users\Andrew\AppData\Roaming\npm`. Текущий процесс Codex унаследовал старый PATH, поэтому разрешение команд по имени может показывать внутренние заглушки; прямые системные пути проверены.
- Официальный реестр npm доступен (`https://registry.npmjs.org/`); в нём подтверждена ветка pnpm 11.x. Corepack доступен, версия `0.36.0`; установка выполнена штатным системным npm с пользовательским префиксом `C:\Users\Andrew\AppData\Roaming\npm`: `npm install --global pnpm@11`.
- В новом чистом процессе подтверждены `C:\Users\Andrew\AppData\Roaming\npm\pnpm.cmd` и `pnpx.cmd`; pnpm `11.26.0`, глобальный список npm также показывает `pnpm 11.26.0`. `pnpx --help` запускается и показывает режим `pnpm dlx`. Команда `pnpm exec` из корня DSH без package.json ожидаемо завершилась `ERR_PNPM_RECURSIVE_EXEC_NO_PACKAGE`; проект не изменён.
- Kaspersky Service 21.26 работает; журнал `Kaspersky Event Log` не содержит записей блокировки.

## Текущий результат

`PASS`: системные Git и Node.js подтверждены, pnpm 11.26.0 установлен в пользовательский npm-префикс и проверен. Следующее единственное действие: открыть новый сеанс PowerShell/терминал, чтобы он унаследовал обновлённый PATH, затем продолжить проверку DSH.

## Установка точной версии DeepSeek Harness — 11.09.2026

- Только чтением проверен официальный реестр npm `https://registry.npmjs.org/`: пакет `@deepseek-ai/dsh`, точная версия `0.1.1-rc.2` существует.
- Проверенные данные публикации: `dist.integrity = sha512-UP1UIh6q3Gme/yXRn/QL2P8IsVlv8Shpg22TRJIZPsCRWLm4CBiA1MUvXmJAfsOEETBMLAl+xWPtFw6ICsN3wg==`; `dist.shasum = 1a5112369f1c46b13a6e6f21de8af5e6afd45074`; команда пакета — `dsh -> lib/bin.js`; обязательное поле `engines` не задано; зависимостей — 62, дополнительных и peer-зависимостей нет.
- До установки в пользовательском глобальном префиксе `C:\Users\Andrew\AppData\Roaming\npm` находился только `pnpm 11.26.0`; конфликтующей версии DSH не было.
- Выполнена штатная установка только `@deepseek-ai/dsh@0.1.1-rc.2` через системный npm `11.19.0` с реестром `https://registry.npmjs.org/`, без аудита и без обновления других пакетов. Установка завершилась без ошибки; предупреждение касается только устаревшего транзитивного `node-domexception@1.0.0`.
- В новом процессе подтверждены файлы `C:\Users\Andrew\AppData\Roaming\npm\dsh.cmd`, `dsh.ps1`, `dsh`, пакет с именем и версией `@deepseek-ai/dsh@0.1.1-rc.2`; `dsh --version` вывел `0.1.1-rc.2`, `dsh --help` загрузился без ошибки. Профиль `C:\Users\Andrew\.dsh\profiles\web`, его `package.json`, lock-файл, настройки, ключи, `node_modules`, Nanmicoder и плагины не изменялись и не читались.
- DSH не запускался как сервер или модель. В системе нет процесса Node.js, запущенного через этот DSH, и нет принадлежащего ему слушающего порта. Работающие процессы Node.js относятся к среде Codex.
- Служба Kaspersky Service 21.26 работает; признаков блокировки установки или запуска справочных команд DSH не обнаружено.

## Текущий результат

`PASS`: точная версия DeepSeek Harness установлена и проверена отдельно от сохранённого профиля. Следующий безопасный этап — сначала подготовить изолированный временный профиль и проверить базовый запуск с пользовательским вводом ключа DeepSeek; старый профиль и Nanmicoder не подключать автоматически.

## Изолированный базовый запуск — 11.09.2026

- Документация установленного Harness и исходник `@deepseek-ai/dsh-home-paths` подтверждают официальный способ изоляции: переменная `DSH_HOME` имеет приоритет над обычным `~/.dsh` и задаёт отдельный корень данных. Команда `dsh --profile web` использует профиль `$DSH_HOME/profiles/web`; встроенный шаблон web содержит только `@deepseek-ai/dsh-base` и `@deepseek-ai/dsh-web-app`.
- Создан отдельный корень `D:\DSH-base-smoke-20260911`. Старые `credentials`, `settings`, профиль, ключи, плагины и `node_modules` в него не копировались; в дереве временного корня файлов `credentials.yaml`, `.credentials.yaml`, `settings.yaml`, `.env` и `.credentials.json` нет.
- В отдельном корне команды `dsh --profile web --dump-default-config` и `dsh web --help` отработали успешно; `web --help` завершился с кодом 0. Временный профиль и служебные ссылки созданы только внутри `D:\DSH-base-smoke-20260911`.
- Для дымовой проверки использован системный `C:\Program Files\nodejs\node.exe` напрямую, чтобы не зависеть от устаревшего PATH процесса Codex. Запуск был скрытым, без открытия браузера и без служб/автозапуска: `dsh web --host 127.0.0.1 --port 4173 --no-open`; режим телеметрии отключён.
- Harness сообщил `dsh web: http://127.0.0.1:4173`; запрос к `http://127.0.0.1:4173/` вернул HTTP 200, HTML-интерфейс размером 14 523 байта. Процесс был `node.exe` из `C:\Program Files\nodejs`, привязка — только `127.0.0.1:4173`; stderr пуст, stdout содержит только строку адреса.
- После проверки процесс завершён штатной остановкой, порт 4173 освобождён, дочерних процессов и оставшегося процесса DSH нет. Журналы проверки: `D:\DSH-base-smoke-20260911\smoke-logs\web.stdout.2.log` и `web.stderr.2.log`.
- Журнал Kaspersky Event Log включён, за последние шесть часов записей нет. В журнале Application за тот же период сообщений о блокировке DSH/Node.js/Kaspersky нет; служба Kaspersky Service 21.26 работает.
- После запуска контрольные SHA-256 сохранённого `C:\Users\Andrew\.dsh\profiles\web\package.json` и `pnpm-lock.yaml` совпали с резервной копией; `settings.yaml` сохранил SHA-256 из защищённого манифеста. Старый профиль не изменён.

## Текущий результат

`PASS`: базовый Harness безопасно запускается в отдельном корне и отдаёт интерфейс по localhost. Ключи, старый профиль, Nanmicoder и плагины не подключались. Следующий этап — отдельная проверка интерфейса и подготовка минимального рабочего профиля; ввод ключа DeepSeek выполнять только пользователю локально, после отдельного подтверждения.

## Полная защищённая копия конфигурации перед дальнейшими изменениями — 11.09.2026

- Создан новый каталог `D:\DSH-recovery-backup-20260911-full` из источника `C:\Users\Andrew\.dsh`.
- Сохранены настройки DSH, `.credentials.yaml` (содержимое не читалось и не выводилось), профиль `web` с основными файлами, небольшой набор конфигураций локальных плагинов и рабочие файлы `tools\deepseek-harness-launcher`.
- Не копировались `node_modules`, кэши, журналы, временные файлы, репозитории Git целиком, большие сборки и модели. Временные `dsh.pid` и журналы запуска исключены.
- В копии 40 файлов, включая манифест и описание копии; 39 записей SHA-256 в `SHA256-MANIFEST.txt`. Общий размер копии: 104519 байт. Размер сверенных файлов источника и копии: 97741 байт.
- Проверка источников и копий: 37 файлов, пропусков нет, несовпадений хэшей и размеров нет. `.credentials.yaml` присутствует в копии, размер 2221 байт; содержимое не показывалось.
- ACL корневой папки отключает наследование и содержит только Andrew, SYSTEM и Administrators; у каждого полный доступ. Папку нельзя перезаписывать без отдельного решения.
- Дополнительные сведения: `D:\DSH-recovery-backup-20260911-full\README-BACKUP.md`, `plugins-inventory.txt`, `SHA256-MANIFEST.txt`.

## Текущий результат

`PASS`: защищённая резервная копия создана и проверена. DSH не запускался, Postman и OpenCode не использовались.

## Чистый постоянный корень и ввод ключа — 11.09.2026

- Перед созданием корня проверена полная копия `D:\DSH-recovery-backup-20260911-full`: 39 записей SHA-256, пропусков нет, несовпадений нет. Содержимое учетных данных не читалось.
- Создан постоянный чистый корень `D:\DeepSeekHarness-Recovered`. В него внесены только профиль web с встроенными пакетами `@deepseek-ai/dsh-base` и `@deepseek-ai/dsh-web-app`, рабочая папка журнала и описание назначения. Старые `settings.yaml`, учетные данные, `cordis.patch.yml`, `node_modules` и плагины не копировались.
- Проверены права каталога: Andrew, SYSTEM и Administrators имеют полный доступ. Секреты в новом корне отсутствуют.
- Системным Node.js напрямую запущен ровно один скрытый процесс DSH с PID `30924` на `127.0.0.1:4173`. HTTP корня вернул 200; основные JavaScript/CSS-файлы вернули 200; stderr пуст; привязка к `0.0.0.0` и IPv6 wildcard не обнаружена. Ключ и модель не вводились.
- Локальный интерфейс не имеет отдельного подтвержденного URL для вкладки Models: Models открывается в панели настроек через значок шестерёнки. Корень интерфейса уже открыт в обычном браузере: `http://127.0.0.1:4173/`.
- Добавлены скрытые средства повторного запуска и остановки: `tools\deepseek-harness-launcher\Recovered-DSH-Start.vbs` и `Recovered-DSH-Stop.vbs`. Полные сценарии находятся рядом в `.ps1`; запуск проверен циклом остановка → запуск, второй экземпляр не оставлен. Состояние процесса хранится в `recovered-dsh-runtime.json` без секретов.

## Текущий результат

`PASS`: постоянный чистый Harness работает на localhost. Пользователю нужно открыть уже показанное окно браузера, нажать шестерёнку, выбрать **Models** и ввести ключ DeepSeek непосредственно в интерфейсе. Ключ в чат не отправлять.

## Локальная проверка после сохранения ключа — 11.09.2026

- Проверка выполнена только локально. Внешние запросы DeepSeek, запрос баланса, список моделей через сеть и `chat/completions` не вызывались.
- В `D:\DeepSeekHarness-Recovered` появился файл `.credentials.yaml`, размер 73 байта. Содержимое не читалось, не декодировалось и не выводилось.
- Права `.credentials.yaml` сужены до Andrew, SYSTEM и Administrators с полным доступом; наследование отключено. Других записей доступа нет.
- Работает ровно один процесс DSH: PID `30924`, `127.0.0.1:4173`. Локальная страница отвечает HTTP 200, внешний интерфейс не слушается.
- Без обращения к сети проверена собранная локальная конфигурация: провайдер `deepseek-official` и модель `deepseek-v4-flash` присутствуют в конфигурации по умолчанию. Команда завершилась успешно и не печатала учетные данные.
- Локальные журналы корня проверены на ошибки сохранения и фатальные ошибки: совпадений не найдено. Данные о действительности ключа и доступности платной модели намеренно не проверялись.

## Текущий результат

`PASS`: ключ сохранён локально, права защищены, Harness и интерфейс работают. Бесплатно доказано только наличие локальной записи и конфигурации; сетевой вызов модели и состояние баланса остаются непроверенными, чтобы не расходовать средства.

## Перенос полного проверенного набора — 11.09.2026

- Перед переносом создан снимок текущего рабочего корня `D:\DeepSeekHarness-Recovered-snapshot-v2-20260911`. Файл `.credentials.yaml` сохранён без чтения; его SHA-256 совпал со снимком, размер 73 байта.
- Профиль и локальные копии Better Sidebar, Restart Web, Flowglass и dshmarket перенесены из кандидата `D:\DSH-full-candidate-v2-20260911` в `D:\DeepSeekHarness-Recovered`. Старые каталоги сохранены как `profiles\web-before-v2-20260911` и `vendor-before-v2-20260911`.
- Новый `package.json` и новый `pnpm-lock.yaml` не содержат ссылок на candidate/staging; локальные file-ссылки указывают на рабочий `vendor`, Flowglass использует внешний проверенный архив `D:\DSH-flowglass-patched-20260911\dsh-flowglass-by-andrew-0.3.0.tgz`.
- Работает ровно один DSH: PID `24980`, `127.0.0.1:4173`; HTTP 200. Клиенты и загрузочные записи Better Sidebar, Restart Web, Flowglass и dshmarket ответили HTTP 200. stderr пуст.
- Локальная конфигурация без сетевого запроса подтверждает `deepseek-official` и `deepseek-v4-flash`. UI магазина, моделей, OAuth и Postman не запускались.

## Текущий результат

`PASS`: рабочий DSH восстановлен с полным проверенным набором расширений и сохранёнными настройками/учётными данными. Текущий адрес: `http://127.0.0.1:4173/`.

## Проверка первых плагинов и безопасный откат — 11.09.2026

- Перед изменением создан снимок чистого рабочего корня: `D:\DeepSeekHarness-Recovered-snapshot-before-plugins`. В него вошли конфигурационные файлы и `.credentials.yaml` без чтения содержимого; журналы, кэши и `node_modules` исключены. Создан `SHA256-MANIFEST.txt`; корневое наследование прав отключено, доступ оставлен Andrew, SYSTEM и Administrators.
- Через официальный `dsh plugin --profile web add` был подключён только локальный `dsh-better-sidebar` версии `0.15.2-andrew.1` как ссылка на сохранённый источник. Исходник не изменялся. При запуске плагин остановил загрузку дерева: в его `lib/index.js` не разрешилась зависимость `ws` (`ERR_MODULE_NOT_FOUND`).
- `dsh-restart-web` версии `1.1.0` не подключался. Nanmicoder, Postman, Flowglass, Branchline и другие плагины не подключались; запросы к модели и внешние вызовы не выполнялись.
- После неуспешной проверки Better Sidebar рабочий профиль откатан к снимку: `profiles\web\package.json` совпадает со снимком по SHA-256, ссылка плагина и созданный lock-файл удалены. Текущий профиль не содержит Better Sidebar или Restart Web.
- Учётные данные в текущем корне и снимке имеют по 73 байта; SHA-256 совпадает. Содержимое не читалось и не выводилось.
- После отката запущен ровно один скрытый DSH с PID `13100`; `http://127.0.0.1:4173/` отвечает HTTP 200, единственный слушатель — `127.0.0.1:4173`, stdout содержит только адрес, stderr пуст.

## Текущий результат

`PARTIAL`: чистый DSH восстановлен и оставлен работающим на localhost; оба дополнительных плагина не подключены. Better Sidebar требует отдельного безопасного восстановления зависимости `ws` и повторной проверки. До этого профиль можно вернуть из `D:\DeepSeekHarness-Recovered-snapshot-before-plugins` без потери текущих учётных данных.

## Подключение Better Sidebar после staging-проверки — 11.09.2026

- Перед изменением рабочего корня создан отдельный снимок `D:\DeepSeekHarness-Recovered-snapshot-before-better-sidebar-20260911` с конфигурацией, профилем и `.credentials.yaml` без чтения содержимого. Кэши, журналы и `node_modules` не копировались; создан `SHA256-MANIFEST.txt`, наследование прав отключено, доступ оставлен Andrew, SYSTEM и Administrators.
- Рабочий DSH был скрыто остановлен. Better Sidebar `0.15.2-andrew.1` подключён официальной командой управления плагинами как ссылка на уже проверенный staging-источник `D:\DSH-plugin-staging-20260911\better-sidebar`; staging содержит `ws@8.21.3`. Исходный код не изменялся, другие плагины не подключались.
- После запуска рабочий DSH имеет ровно один PID `3772`, слушает только `127.0.0.1:4173`, HTTP 200. Маршруты Better Sidebar зарегистрированы: `/sidebar/api` → 405, `/sidebar/file` → 400, `/sidebar/bundle/nope.js` → 404. stdout содержит только адрес, stderr пуст.
- `.credentials.yaml` в рабочем корне и новом снимке имеет по 73 байта и совпадающий SHA-256; содержимое не читалось и не выводилось. Конфигурация модели сохранилась: провайдер `deepseek-official`, модель `deepseek-v4-flash`. Запросов модели и баланса не выполнялось.

## Текущий результат

`PASS`: Better Sidebar подключён и загружается в рабочем Harness с точной зависимостью `ws@8.21.3`; рабочий DSH стабильно работает на localhost. Для отката использовать новый снимок `D:\DeepSeekHarness-Recovered-snapshot-before-better-sidebar-20260911`. Restart Web и остальные плагины пока не подключать.

## Изолированная проверка и подключение Restart Web — 11.09.2026

- В staging-профиле без credentials к текущему набору Base + Web + Better Sidebar добавлен только `dsh-restart-web` версии `1.1.0`. Конфигурационный снимок содержит строку `dsh-restart-web`; клиентский файл `/plugins/dsh-restart-web/client.js` ответил HTTP 200. Staging запущен на `127.0.0.1:4174`, HTTP 200, stderr пуст, после проверки остановлен; порты `4174–4199` свободны.
- Для staging не создавались и не читались credentials. Запросов к модели и балансу не выполнялось.
- В staging при проверке служебного пути был получен HTTP 500; действие перезапуска не нажималось, POST не выполнялся. Основной DSH на тот момент сохранил PID `3772`; после этого рабочий процесс был штатно остановлен перед изменением и запущен заново.
- Перед изменением рабочего профиля создан новый снимок `D:\DeepSeekHarness-Recovered-snapshot-before-restart-web-20260911` с конфигурацией, профилем и `.credentials.yaml` без чтения содержимого. Кэши, журналы и `node_modules` не копировались; создан `SHA256-MANIFEST.txt`, наследование прав отключено, доступ оставлен Andrew, SYSTEM и Administrators.
- В рабочий профиль Restart Web подключён официальной командой управления плагинами, Better Sidebar сохранён. Функция перезапуска через пользовательский интерфейс и служебный POST не вызывались.
- Рабочий DSH работает ровно одним PID `32908`, слушает только `127.0.0.1:4173`, HTTP 200. Маршруты Better Sidebar: `/sidebar/api` → 405, `/sidebar/file` → 400, `/sidebar/bundle/nope.js` → 404. Клиент Restart Web `/plugins/dsh-restart-web/client.js` → 200. stderr пуст.
- `.credentials.yaml` в рабочем корне и новом снимке имеет по 73 байта и совпадающий SHA-256; содержимое не читалось и не выводилось. Конфигурация модели сохранена: `deepseek-official / deepseek-v4-flash`. Внешние запросы не выполнялись.

## Текущий результат

`PASS`: Restart Web `1.1.0` подключён в рабочий Harness и загружается вместе с Better Sidebar; интерфейс работает на localhost. Для отката использовать `D:\DeepSeekHarness-Recovered-snapshot-before-restart-web-20260911`. Кнопку перезапуска не нажимать во время дальнейших проверок.

## Подключение Codex/OpenAI из проверенного staging — 11.09.2026

- Перед изменением создан снимок `D:\DeepSeekHarness-Recovered-snapshot-codex-20260911` с профилем, конфигурацией и копией `.credentials.yaml`; содержимое учетных данных не читалось. SHA-256 рабочего `.credentials.yaml` совпал со снимком: `A50C0D6627C1D0C7B26D9C4F6B7777CEEC8D33417669205659F6CA180FC87F94`.
- В рабочий профиль перенесены только `dsh-codex-oauth@0.1.6` из commit `362738416bccddcb7a882ccf4c98f2ac2959e61f` и `@earendil-works/pi-ai@0.85.1`; существующие Better Sidebar и Restart Web сохранены.
- Провайдер по умолчанию не изменён: `deepseek-official / deepseek-v4-flash`.
- После скрытого перезапуска рабочий DSH имеет один реальный процесс, слушает только `127.0.0.1:4173`, HTTP возвращает 200, stderr пуст. `dsh --dump-config` видит `dsh-codex-oauth`.
- Локальный каталог `pi-ai@0.85.1` показывает провайдер `openai-codex` и модель `gpt-6-astra`; сеть модели, OAuth и баланс не проверялись.

`PASS`: техническое подключение выполнено и проверено без вызова модели. Следующий ручной шаг — открыть Harness в Chrome, в настройках выбрать OAuth для `openai-codex`, пройти вход кодом устройства и после перезапуска проверить выбор `gpt-6-astra`. Ключи и коды в чат не отправлять.

## Проверка dshmarket и откат — 11.09.2026

- В изолированном staging-профиле без credentials подготовлен dshmarket `1.19.0` из сохранённого собранного пакета. После добавления локальных runtime-зависимостей (`undici@7.29.0`, `js-yaml@4.3.1`, `@deepseek-ai/cosmokit@1.8.2`, совместимые `dsh-settings`/`cordis`) staging успешно запустился на `127.0.0.1:4174`: HTTP 200, `/plugins/dshmarket/client.js` → 200, stderr пуст. Запросы к `/dsh-market/registry` и любые действия магазина не выполнялись.
- Перед рабочей попыткой создан снимок `D:\DeepSeekHarness-Recovered-snapshot-before-dshmarket-20260911`; credentials в корне и снимке совпадают по размеру и SHA-256, содержимое не читалось.
- При подключении dshmarket к рабочему профилю официальный менеджер плагинов автоматически подхватил ранее подготовленные параллельные пакеты `dsh-codex-oauth` и `@earendil-works/pi-ai`. Запуск рабочего дерева остановился на отсутствующей для этого параллельного пакета `@deepseek-ai/schemastery`; dshmarket отдельно не был причиной ошибки загрузки.
- Рабочая конфигурация откатана к стабильному набору Base + Web + Better Sidebar + Restart Web; строка dshmarket и его ссылка удалены. Параллельные каталоги не удалялись. После отката DSH запущен скрыто: один PID `27752`, только `127.0.0.1:4173`, HTTP 200, ошибки запуска отсутствуют.
- Текущий рабочий пакет не содержит dshmarket; Better Sidebar и Restart Web сохранены. Модель `deepseek-official / deepseek-v4-flash` и credentials не менялись. Внешние запросы и вызовы модели не выполнялись.

## Текущий результат

`PARTIAL/BLOCKED`: dshmarket доказан в изолированном staging, но в рабочий профиль безопасно не перенесён из-за обнаруженного параллельного набора пакетов и ошибки его зависимости. Рабочий DSH восстановлен на `127.0.0.1:4173`; для дальнейшего переноса сначала нужно отдельно согласовать и исправить конфликт параллельного Codex-пакета.

## Кандидат обычных плагинов — остановлено по указанию — 11.09.2026

- Рабочий профиль не изменялся. Создан отдельный кандидат `D:\DSH-registry-plugins-candidate-20260911` без `.credentials.yaml`; в нём подготовлен базовый набор Base + Web + Better Sidebar + Restart Web.
- В кандидат скопированы локальные источники для последовательной проверки: `dshmarket 1.19.0`, `@anionex/dsh-turn-rewind 0.1.2`, `dsh-notification 0.1.1`, `dsh-client-auto-continue 0.8.1`, `dsh-tokenledger 0.1.0`. Исходники не изменялись.
- По команде остановлено до запуска тестов. Staging-процессов и слушателей на `4174–4199` не осталось. Запросов к моделям, магазину, балансу и внешним службам не выполнялось.

| Плагин | Версия | Результат |
|---|---:|---|
| dshmarket | 1.19.0 | не проверен в этом кандидате |
| @anionex/dsh-turn-rewind | 0.1.2 | не проверен |
| dsh-notification | 0.1.1 | не проверен |
| dsh-client-auto-continue | 0.8.1 | не проверен; paused не задавался |
| dsh-tokenledger | 0.1.0 | не проверен; disabled не задавался |

`PARTIAL`: кандидат подготовлен, но проверяемых PASS/FAIL для пяти плагинов нет. Отдельно обнаружено, что текущая рабочая запись `profiles\web\package.json` всё ещё содержит `dshmarket`, тогда как его ссылка отсутствует; из-за этого штатный запуск рабочего DSH завершился ошибкой разрешения пакета. Рабочий профиль намеренно не исправлялся в рамках остановленной задачи.

## Кандидат: dshmarket — staging PASS — 11.09.2026

- Рабочий `D:\DeepSeekHarness-Recovered` не изменялся. В кандидате `D:\DSH-registry-plugins-candidate-20260911` выполнена штатная установка профиля командой DSH `plugin --profile web install`; первый скрытый запуск pnpm был остановлен отсутствием окна подтверждения, повтор с `CI=true` завершился успешно.
- Проверялся только `dshmarket 1.19.0`. Для его прямых зависимостей зафиксированы `js-yaml 4.3.1` и `undici 7.29.0`. Транзитивная зависимость `js-yaml → argparse` разрешилась локальной версией `argparse 2.0.1`. Обязательные узлы среды DSH: `@deepseek-ai/cordis 4.0.2`, `@deepseek-ai/dsh-settings 0.1.1-rc.2`, `@deepseek-ai/dsh-client-connection`, `dsh-client-runtime`, `dsh-client-locale`, `dsh-client-ui-settings`, `dsh-client-ui-theme` — все `0.1.1-rc.2` из установленного DSH.
- `@deepseek-ai/schemastery` для самого dshmarket не является обязательной зависимостью: в его `package.json` она отсутствует среди dependencies/peerDependencies. В установленном DSH она присутствует как `3.18.2`; отдельно в пакет dshmarket её не добавляли.
- Staging запущен скрыто на `127.0.0.1:4174`: корень → HTTP 200; запись `dshmarket` присутствует в `__DSH_BOOT__`; `/plugins/dshmarket/client.js` → HTTP 200; stderr пуст. Слушатель был единственным и только loopback. Маршруты магазина, каталог, установка плагинов, баланс и любые модельные запросы не вызывались.
- После проверки staging остановлен; слушателей и процессов DSH на `4174` не осталось.

`PASS (staging only)`: dshmarket загружается в изолированном кандидате. В рабочий профиль не переносился.

## Кандидат: dshmarket + @anionex/dsh-turn-rewind — staging PASS — 11.09.2026

- Рабочий `D:\DeepSeekHarness-Recovered` не изменялся. В уже проверенный кандидат штатной командой DSH добавлен только `@anionex/dsh-turn-rewind 0.1.2`; его `package.json` и локальный собранный `lib` проверены.
- У `@anionex/dsh-turn-rewind` нет runtime dependencies. Обязательная среда: peer `@deepseek-ai/cordis 4.0.2`; клиентские узлы `@deepseek-ai/dsh-client-runtime` и `@deepseek-ai/dsh-client-ui-conversation` версии `0.1.1-rc.2` предоставлены установленным DSH. Версия Node соответствует требованию пакета: `24.21.0`.
- Совместный staging запуск выполнен скрыто на `127.0.0.1:4174`. Корень → HTTP 200; в `__DSH_BOOT__` присутствуют `dshmarket` и `@anionex/dsh-turn-rewind`; `/plugins/dshmarket/client.js` → 200; `/plugins/@anionex/dsh-turn-rewind/client.js` → 200; Better Sidebar также → 200. Служебный GET `/turn-rewind` ответил 409 (маршрут существует, запрос без выбранной последовательности отклонён); операции восстановления, записи файлов и создания дочерних задач не выполнялись.
- stderr обоих запусков пуст, bind только loopback. Первый процесс исчез после начальной проверки без ошибки; повторный запуск оставался жив не менее 5 секунд и прошёл все проверки. После этого staging остановлен, порт `4174` свободен.
- dshmarket сохранил прямые зависимости `js-yaml 4.3.1` и `undici 7.29.0`; его peer/client узлы остались без изменений. Запросы к каталогу, моделям, балансу и Postman не выполнялись.

`PASS (staging only)`: dshmarket и turn-rewind совместно загружаются. В рабочий профиль не переносились.

## Кандидат: dshmarket + turn-rewind + dsh-notification — staging PASS — 11.09.2026

- Рабочий `D:\DeepSeekHarness-Recovered` не изменялся. В кандидат штатной командой DSH добавлен только `dsh-notification 0.1.1`; предыдущие dshmarket `1.19.0`, turn-rewind `0.1.2`, Better Sidebar и Restart Web сохранены.
- `dsh-notification` не имеет runtime dependencies; обязательный peer `@deepseek-ai/schemastery ^3` разрешён предоставленным установленным DSH `@deepseek-ai/schemastery 3.18.2`. Клиентский узел использует `@deepseek-ai/dsh-client-runtime 0.1.1-rc.2`.
- Единственный запуск выполнен скрыто на `127.0.0.1:4174`: корень → HTTP 200; в `__DSH_BOOT__` присутствуют `dshmarket`, `@anionex/dsh-turn-rewind` и `dsh-notification`; клиенты dshmarket, turn-rewind, notification и Better Sidebar → HTTP 200. В исходнике notification серверных маршрутов нет — плагин наблюдает события; служебные маршруты других плагинов не вызывались.
- stderr чистый, listener только loopback. Браузерный код не исполнялся, разрешение уведомлений не запрашивалось, тестовое уведомление и события завершения/ошибки/подтверждения не создавались.
- После проверки staging остановлен, процессы и порт `4174` свободны. Модели, баланс, каталог магазина и Postman не вызывались.

`PASS (staging only)`: dsh-notification совместно загружается с предыдущим набором. В рабочий профиль не переносился.

## Кандидат: dshmarket + turn-rewind + notification + auto-continue — staging PASS — 11.09.2026

- Рабочий `D:\DeepSeekHarness-Recovered` не изменялся. В кандидат штатной командой DSH добавлен только `dsh-client-auto-continue 0.8.1`; предыдущие плагины сохранены.
- Безопасное поле конфигурации: `D:\DSH-registry-plugins-candidate-20260911\settings.yaml` → `auto-continue.paused: true`. Дополнительно установлены `scanOnBoot: false` и `notify: false`. У плагина нет отдельного обязательного поля `disabled`; добавление `disabled: true` отключило бы сам объект, поэтому для проверки загрузки применён штатный глобальный pause.
- Дерево зависимостей auto-continue: peer `react ^18.2.0`, `@deepseek-ai/dsh-settings ^0.1.0-rc.7`, `@deepseek-ai/schemastery ^3.18.1`, `@deepseek-ai/cordis ^4.0.1`; клиентские узлы `dsh-client-connection`, `dsh-client-runtime`, `dsh-client-locale`, `dsh-client-ui-settings`, `dsh-client-ui-settings-plugins` — версия установленного DSH `0.1.1-rc.2`. Runtime dependencies отсутствуют.
- Staging запущен скрыто на `127.0.0.1:4174`: корень → HTTP 200; в `__DSH_BOOT__` присутствуют dshmarket, turn-rewind, notification, Better Sidebar, Restart Web и auto-continue; клиенты dshmarket, turn-rewind, notification, auto-continue и Better Sidebar → HTTP 200. Read-only GET `/api/auto-continue-bridge` вернул состояние со счётчиком `sent: 0`; action route не вызывался.
- stderr чистый, listener только loopback. Сообщения, модельные запросы, сканирование сессий, разрешение браузерных уведомлений и тестовые уведомления не создавались. После проверки staging остановлен, порт `4174` свободен.

`PASS (staging only)`: auto-continue совместно загружается и остаётся глобально приостановленным (`auto-continue.paused: true`). В рабочий профиль не переносился.

## Кандидат: обычная группа + dsh-tokenledger disabled — staging PASS — 11.09.2026

- Рабочий `D:\DeepSeekHarness-Recovered` не изменялся. В кандидат штатной командой DSH добавлен только `dsh-tokenledger 0.1.0`; предыдущие плагины сохранены.
- Точный способ отключения взят из старого рабочего `cordis.patch.yml`: в `D:\DSH-registry-plugins-candidate-20260911\profiles\web\cordis.patch.yml` добавлена запись `- id: tokenledger` с `disabled: true`. Пакет остаётся в зависимостях/списке наборов, но его entry не активируется.
- Единственная runtime-зависимость tokenledger — необязательный peer `@deepseek-ai/schemastery ^3.18.1`; в установленной среде доступен `@deepseek-ai/schemastery 3.18.2`. Клиентские узлы: DSH locale, runtime и UI primitives `0.1.1-rc.2`. Дополнительных runtime dependencies нет.
- Staging запущен скрыто на `127.0.0.1:4174`: корень → HTTP 200; manifest содержит dshmarket, turn-rewind, notification, auto-continue, Better Sidebar и Restart Web, но не содержит tokenledger; клиенты всех активных плагинов → HTTP 200. `/plugins/dsh-tokenledger/client.js` → 404 ожидаемо из-за `disabled: true`.
- stderr пуст, listener только loopback. Файлы `tokenledger.sqlite`, `-wal` и `-shm` в кандидате не создавались, значит подсчёт не запускался. Модели, каталог, баланс и Postman не вызывались.
- После проверки staging остановлен, порт `4174` свободен.

`PASS (staging only)`: явное `disabled: true` не мешает остальным плагинам. TokenLedger не активирован и в рабочий профиль не переносился.
## Flowglass — подключение 11.09.2026

- Перед изменением создан снимок `D:\DeepSeekHarness-Recovered-snapshot-flowglass-20260911` с package/config и метаданными `node_modules`; содержимое credentials не читалось, SHA-256 сохранён.
- Архив `D:\DSH-flowglass-patched-20260911\dsh-flowglass-by-andrew-0.3.0.tgz` подключён как постоянный локальный пакет `D:\DeepSeekHarness-Recovered\vendor\dsh-flowglass-by-andrew`, версия `0.3.0`.
- Добавлен официальный `@deepseek-ai/dsh-typert-protocol@0.1.1-rc.2`, без псевдонимов. Codex и dshmarket не добавлялись.
- После скрытого запуска рабочий Harness отвечает `HTTP 200` на `127.0.0.1:4173`; слушатель только loopback, один процесс, stderr пуст. Better Sidebar и Restart Web сохранены.
- Проверены локальные package metadata Flowglass и typert protocol. Прямые URL старых маршрутов плагина возвращают 404; требуется отдельная проверка UI внутри Harness.
- credentials SHA-256 совпадает с предшествующим снимком; default DeepSeek не изменялся. Вызовы OAuth и моделей не выполнялись.

`PASS`: Flowglass подключён и рабочий DSH стабильно поднят; следующий шаг — ручная проверка панели Flowglass в Chrome.
## Проверка dshmarket 1.19.0 — 11.09.2026

- Перед изменением создан снимок `D:\DeepSeekHarness-Recovered-snapshot-dshmarket-20260911`; credentials не читались, сохранена только SHA-256.
- В отдельном кандидате `D:\DSH-dshmarket-candidate-20260911` подключён проверенный `dshmarket@1.19.0` и выполнен запуск на `127.0.0.1:4174`.
- HTTP 200 получен, но stderr кандидата содержит ошибку загрузки Better Sidebar: отсутствует `ws`. Поэтому перенос в рабочий профиль не одобрен.
- Кандидатный процесс остановлен и порт 4174 освобождён. Рабочий Harness не изменён и снова проверен: один процесс на `127.0.0.1:4173`, HTTP 200.

`NO-GO`: dshmarket пока не подключён; сначала нужно отдельно восстановить полное дерево зависимостей Better Sidebar в кандидате.
## Повторная проверка dshmarket с ws@8.21.3 — 11.09.2026

- В отдельный кандидат добавлен точный `ws@8.21.3`; установка прошла без скриптов.
- При запуске кандидата на `127.0.0.1:4174` Better Sidebar всё равно не разрешил `ws` из постоянного каталога плагина: `ERR_MODULE_NOT_FOUND`; HTTP 200 и пустой stderr не достигнуты.
- Кандидат остановлен, рабочий профиль не изменён. `127.0.0.1:4173` снова проверен: один процесс, HTTP 200.

`NO-GO`: dshmarket не переносить. Требуется отдельное самодостаточное дерево постоянного Better Sidebar, а не добавление ещё одной корневой зависимости.

## Nanmicoder — exact commit и изолированный staging 11.09.2026

- Рабочий профиль `D:\DeepSeekHarness-Recovered` не изменялся. В локальном репозитории `C:\Users\Andrew\.dsh` подтверждены ветка `feature/nanmicoder-by-andrew-foundation` и exact commit `4ed2b866ae56faa51ba8945d18f18ccd715d0fcc` (`fix: commit Nanmicoder runtime artifacts`).
- Exact commit извлечён через `git archive` в `D:\DSH-nanmicoder-rebuild-20260911`. Пакет `dsh-agent-teams-nanmicoder-by-andrew` подтверждает версию `0.1.10-andrew.1`; lock-файл принят, зависимости установлены воспроизводимо по `--frozen-lockfile`, `pnpm run build` прошёл.
- Собран tgz `D:\DSH-nanmicoder-rebuild-20260911\artifacts\dsh-agent-teams-nanmicoder-by-andrew-0.1.10-andrew.1.tgz`, SHA-256: `DBAE81E3743A2802429E95B8070F4AA68CAF9C365E8C6E466FF3DAD5B1494632`. Повторная упаковка дала тот же SHA.
- В отдельном `D:\DSH-nanmicoder-staging-20260911` без `.credentials.yaml` подключены только Base, Web и этот tgz. На `127.0.0.1:4174` корень и `/plugins/dsh-agent-teams-nanmicoder-by-andrew/client.js` вернули HTTP 200; manifest/HTML содержит регистрацию Nanmicoder; вызовы моделей и действия AgentTeams не выполнялись. После проверки staging остановлен, порт свободен.
- В журнале запуска были шесть сообщений Windows о ненайденном `npx`; поиск по собранному Nanmicoder вызовов `npx` не обнаружил. Это оставлено как отдельная оговорка для следующего чистого запуска, рабочий профиль не затронут.

`PASS (артефакт и изолированный staging; подключение в рабочий профиль не выполнялось)`: подробности и хэши — в [отчёте Nanmicoder](D:/DSH-nanmicoder-rebuild-20260911/NANMICODER-REPORT-2026-09-11.md).

## Кандидат v3 и перенос в рабочий профиль — PASS — 11.09.2026

- Отдельный кандидат `D:\DSH-full-candidate-v3-20260911` собран без credentials/settings. Новый package/lock содержит turn-rewind `0.1.2`, notification `0.1.1`, auto-continue `0.8.1`, tokenledger `0.1.0`, Better Sidebar `0.15.2-andrew.1`, Restart Web `1.1.0`, Flowglass `0.3.0`, dshmarket `1.19.0` и `ws 8.21.3`; `node-pty` собран штатно.
- В patch закреплены `auto-continue.paused: true`, `scanOnBoot: false`, `notify: false`; entry `tokenledger` отключён через `disabled: true`.
- Кандидат скрыто запускался на `127.0.0.1:4176`: HTTP 200, 49/49 клиентских модулей — 200, bridge вернул `sent: 0`, TokenLedger — ожидаемый 404, база не создавалась, stderr пуст. Кандидат остановлен.
- Перед переносом создан снимок `D:\DeepSeekHarness-Recovered-snapshot-v3-20260911` с profile/vendor/settings и credentials; содержимое credentials не читалось, сохранён только SHA-256.
- В рабочем `D:\DeepSeekHarness-Recovered` старые каталоги сохранены как `profiles\web-before-v3-live-20260911` и `vendor-before-v3-live-20260911`; credentials/settings не заменялись. Рабочий profile установлен по новому lock-файлу.
- Рабочий DSH скрыто запущен на `127.0.0.1:4173`, единственный слушатель PID `32328`, HTTP 200. Все 49 клиентских модулей — 200; Better Sidebar, Restart Web, Flowglass, dshmarket, turn-rewind, notification и auto-continue загружены. TokenLedger отсутствует в клиентском манифесте и отключён.
- Локальный dump подтвердил default DeepSeek `deepseek-official/deepseek-v4-flash`; auto-continue приостановлен. Credentials SHA-256: `A50C0D6627C1D0C7B26D9C4F6B7777CEEC8D33417669205659F6CA180FC87F94`. Модели, баланс, магазин, OAuth и Postman не вызывались.

`PASS`: v3 перенесён в рабочий профиль; рабочий сервер оставлен на `http://127.0.0.1:4173`.
