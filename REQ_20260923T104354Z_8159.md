# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260923T104354Z_8159
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: d0d4a77884b4c05d4216f341ae791f789b0b9a16

## User intent

Уточни критические недоказанные границы из твоего ответа по rc.2/current repo, не реализуя. 1) requestId есть в trusted receipt, но КАК host докажет, что именно текущему Leader session принадлежит этот Postman RESULT_DURABLE, а не чужому Leader, если один и тот же root доступен всем? Есть ли persisted связь parentLeaderSessionId→REQ в postman_bridge/direct-current-turn или только Bridge childSessionId? Нужно ли новое host-scoped grant registry при bridge terminal и как пережить restart; запрети любую подмену requestId task текстом. 2) Чтение: exact rc.2 FS read/glob/grep, web_fetch, web_search могут вытекать на sibling workspace? Можно ли оставить имена read/glob/grep и заменить registrations child-own fenced facade, или требуются worker_read и removal стандартных tools? Учти browser screenshot image attachment, session export. 3) ZIP: как не получить TOCTOU при чтении receipt/sidecars, archive private copy, и какое правило если same REQ used twice concurrently? 4) evidence: кто задаёт обязательные assertions, модель или Leader в structured spec? Иначе Worker может запросить только лёгкие checks и получить completed. Дай конкретный contract и supported signatures/paths; отмечай неизвестное. Ответ <=4000 либо file-mode.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260923T104354Z_8159>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260923T104354Z_8159>>>
