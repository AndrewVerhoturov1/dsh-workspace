Внедри `POSTMAN_SKILL_V12_RESUME_CLICKABLE.zip` строго по `LUNA_INSTRUCTIONS.md`. Это локальная доработка инструктивного слоя Postman: Direct Postman/Ch1/ORCA не запускать, новый REQ не создавать. Skill v12 должен сделать `resume_request.ps1` единственным normal local-finalization entrypoint после RESULT_DURABLE; normal test — только argv-safe TestScript/TestSpec, без python -c и TestCommand; финальный отчёт — authoritative changedFiles кликабельными в Harness Web как Markdown inline code с exact retained worktree path. Runtime не менять. Сначала соблюди REPO_POLICY и не трогай PR #97 без отдельной команды. Нужны targeted contract test + полный Postman regression + git diff --check, 0 failed. Если policy позволяет — один PR в main, не merge; если блокирует — STOP и отчитай blocker.
