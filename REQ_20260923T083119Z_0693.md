# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260923T083119Z_0693
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: a8884bbea93e1cdbb36794b1b02a4bdd1bf2cd4c

## User intent

Финальный gap review. Исправленный ответ всё ещё пропустил локальный static preview и click/console/screenshot, а browser_preview запрещает local URL, хотя задача требует QA локального URL. Исследуй rc.2/workspace по точным API/тестам и спроектируй реалистичный V1: worker-owned preview_static (без shell) + browser_open/snapshot/click/console/errors/screenshot на только preview-issued loopback handle; persistent preview отдельно. Укажи как sandbox read-only совмещается с браузерным temp profile и портом (host-owned process вне agent sandbox, explicit fence), Windows no console window, image/attachment handling и cleanup. Проверь security safe web_fetch DNS rebinding/redirect с Node fetch (если нельзя гарантировать network pin, не обещай SSRF safety: предложи отложить web_fetch либо надежный transport). Проверь живой preset switching: agent/created + agent-preset/selected timing и снятие worker_run own registration. Укажи точные existing paths/symbols для будущих файлов, остановись на доказанном. Без кода/изменений. Ответ можно file-mode.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260923T083119Z_0693>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260923T083119Z_0693>>>
