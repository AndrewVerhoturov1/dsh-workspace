# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260923T111243Z_2797
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: 9cd96b157bc2dee165c32dbf4ee2536d25d22562

## User intent

Разреши два противоречия, проверяя rc.2/workspace, без кода. (A) Ранее предлагалось parent.session.append + flush + persistence как durable grant authority; сейчас ты говоришь host-owned durable state, event только audit. Выбери архитектуру с сохранением после restart, изоляцией разных Leader, безопасной конкурентной выдачей без изменения Bridge Luna transport. Есть ли existing host-owned store с atomic writes/locking или нужен новый? Grant ID не secret, exact owner check. Если один grant/REQ запускает два Workers ОДНОГО Leader параллельно, разрешаем ли multi-use с независимыми lease; не предлагай one-shot consumption без требования. (B) browser spec: предложи небольшой точный typed V1 для click+text_equals; что выдаёт host в evidence, если UI changed, click blocked, target отсутствует; required assertion expected не должен подменяться Worker. Как выбрать action target без arbitrary JS? (C) file fences: абсолютные пути и symlink/race/rg; можно ли гарантировать Worker B не прочитает C при same Windows user без ОС изоляции? Честно раздели capability-level isolation и adversarial process-level, назови ограничения. Укажи exact evidence и остающееся неизвестное.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260923T111243Z_2797>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260923T111243Z_2797>>>
