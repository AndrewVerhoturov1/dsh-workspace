# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260923T085533Z_5389
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: d6610ec2e29f4c999c29209f0005bb7a17be99af

## User intent

Проведи короткий независимый критический обзор предложенного плана на DSH rc.2 (upstream aa6c361a..., workspace latest). Не реализация. Проверь 4 сомнения с exact paths/symbols/tests: (1) может ли custom provider действительно применить read-only/approval never через public appendDelegatedPolicyOverrides без нарушения parent stronger policy; (2) ctx.attachments.saveImage точная signature и пригодность image content block для one-shot tool output/session export; (3) own-scope worker_run top-level Leader: наследуется ли Agent-to-Agent, если child via composeFrom, достаточно ли исполнения guard и tool restriction для не-видимости Bridge; (4) установка отдельного plugins/dsh-postman-worker в web profile: точный механизм bundle, profile package lock и plugin discovery, либо проще в existing package. Верни только факты, рекомендации и остаточные риски <=3900 символов.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260923T085533Z_5389>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260923T085533Z_5389>>>
