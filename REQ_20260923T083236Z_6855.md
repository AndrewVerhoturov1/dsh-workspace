# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260923T083236Z_6855
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: ad57fa485ab741edc279c6c8001ef20702d16fe0

## User intent

Проведи независимое read-only исследование пробелов для отдельного локального postman-worker на DSH 0.1.1-rc.2; прежний continuation получил PROMPT_SEND_UNKNOWN, не повторяй его REQ. Bridge остаётся неизменным transport spawn. Установи по коду/tests workspace и rc.2: (1) узкий host-owned static preview без shell, loopback port 0, root containment, lifetime; (2) browser QA только по preview-issued handle с open/snapshot/click/console/errors/screenshot, как владеть Playwright процессом и temp profile вне read-only Agent sandbox, Windows no console, attachments; (3) SSRF-safe web_fetch: достаточно ли Node fetch с DNS preflight при rebinding/redirect? Если нет, предложи рабочий pinned-address transport или честно отложи fetch; (4) timing agent/created и agent-preset/selected для own-scope worker_run и переключения preset; (5) точные файлы и тесты. Факты против архитектурных рекомендаций, не изменяй ничего. Ответ можно большим.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260923T083236Z_6855>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260923T083236Z_6855>>>
