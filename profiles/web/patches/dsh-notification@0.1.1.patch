diff --git a/index.js b/index.js
index f5c62dce79f49db2721a535d3aa665e835e68aef..68e1f90b6a92f34734bb2d61571dc3bf56ea3cb3 100644
--- a/index.js
+++ b/index.js
@@ -46,8 +46,15 @@ function desktopNotify(title, body) {
         `$t.GetElementsByTagName('text').Item(1).AppendChild($t.CreateTextNode('${esc(body)}')) | Out-Null;`,
         `[Windows.UI.Notifications.ToastNotificationManager]::CreateToastNotifier('${esc(title)}').Show([Windows.UI.Notifications.ToastNotification]::new($t));`,
       ].join(' ')
-      spawn('powershell', ['-NoProfile', '-NonInteractive', '-Command', ps],
-        { stdio: 'ignore', detached: true }).unref()
+      spawn('powershell', [
+        '-NoLogo',
+        '-NoProfile',
+        '-NonInteractive',
+        '-WindowStyle',
+        'Hidden',
+        '-Command',
+        ps,
+      ], { stdio: 'ignore', detached: true, windowsHide: true }).unref()
     }
   } catch {}
 }
