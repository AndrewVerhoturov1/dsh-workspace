diff --git a/lib/index.js b/lib/index.js
index 43eafd8ccb13da1ecef4d94abd92fe19a2174899..4b62ba4f544a27d30bb9f23862a42f15f74f3918 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -133,6 +133,19 @@ var SandboxPwshExecutor = class extends PwshLocalExecutor {
 		super(ctx, config);
 		this.mode = ctx.sandboxPolicy.defaultMode;
 	}
+	/** Keep sandboxed background PowerShell invocations windowless as well. */
+	argv(spec) {
+		const argv = super.argv(spec);
+		if (process.platform !== "win32" || argv.includes("-WindowStyle")) return argv;
+		const commandIndex = argv.indexOf("-Command");
+		if (commandIndex === -1) return argv;
+		return [
+			...argv.slice(0, commandIndex),
+			"-WindowStyle",
+			"Hidden",
+			...argv.slice(commandIndex)
+		];
+	}
 	/** The configured default mode — the capability fact the tool layer reads. */
 	get sandboxMode() {
 		return this.mode;
