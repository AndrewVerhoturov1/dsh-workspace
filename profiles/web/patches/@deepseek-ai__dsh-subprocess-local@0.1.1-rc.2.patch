diff --git a/lib/index.js b/lib/index.js
index 698efc9357d249cf03adf82a9962499f9455fe66..e60c7740d18ba35a2355cdde2b2ccdff69138ab9 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -106,7 +106,7 @@ function taskkillTree(pid, force) {
 		String(pid),
 		"/T",
 		...force ? ["/F"] : []
-	], { stdio: "ignore" });
+	], { stdio: "ignore", windowsHide: true });
 }
 /**
 * True for NULL and INVALID_HANDLE_VALUE returns from Win32 handle APIs.
@@ -746,7 +746,7 @@ function taskkillProcessTree(pid) {
 		String(pid),
 		"/T",
 		"/F"
-	], { stdio: "ignore" });
+	], { stdio: "ignore", windowsHide: true });
 }
 /**
 * Signal a detached process tree with platform-correct semantics: POSIX
@@ -802,7 +802,8 @@ function spawnSubprocess(spec, internals = {}) {
 			outMode === "inherit" ? "inherit" : "pipe",
 			errMode === "inherit" ? "inherit" : "pipe"
 		],
-		detached: platform !== "win32"
+		detached: platform !== "win32",
+		windowsHide: platform === "win32"
 	});
 	const collectStream = (mode, stream, label) => {
 		if (!isCollect(mode) || stream === null) return void 0;
@@ -1295,7 +1296,8 @@ var LocalSubprocessRuntime = class extends SubprocessRuntime {
 			rows: spec.rows,
 			cols: spec.cols,
 			cwd: spec.cwd,
-			env: childEnv(spec.env)
+			env: childEnv(spec.env),
+			...(process.platform === "win32" ? { useConpty: true } : {})
 		};
 		const inspector = this.terminalInspector ?? createProcessInspector();
 		const handle = new LocalTerminalHandle(nodePty.spawn(file, [...spec.argv.slice(1)], options), inspector, spec.graceMs);
