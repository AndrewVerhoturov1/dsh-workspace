# POSTMAN ASK TASK

protocol_version: 1
request_id: REQ_20260922T234417Z_4045
result_mode: text
repository: AndrewVerhoturov1/dsh-workspace
base_commit: 874db4abd12232f6a5bb2255607c92ddce48a8ff

## User intent

Заверши архитектурное исследование точечной запиской по dsh-workspace и upstream master. Inline до 3900 символов. Нужны: (1) exact repo files, которые придётся менять/добавить для worker_run, postman-worker provider/preset, host-owned terminal status, browser facade, preview tool, UI/docs/tests; (2) предложенный строгий worker_run input/output schema и trusted terminal outcome protocol, объясни, почему обычный SubagentResult.output недостаточен или достаточен и как избежать доверия child prose; (3) test matrix и один реалистичный E2E от Leader до видимого child, preview/browser QA, результата, export и cleanup; (4) конфликт/сосуществование с bridge; (5) только действительно оставшиеся неизвестные. Проверь фактические нынешние naming/registration/plugin entrypoints/tests, не выдумывай paths: для новых файлов пометь NEW, существующие подтверди symbol/path/SHA. Не реализуй. Раздели доказанные факты, вывод и рекомендацию.

## Execution contract

- Этот task-файл self-contained; выполнить `User intent` буквально.
- `repository` и `base_commit` — только transport/correlation metadata.
- GitHub использовать только как READ source, если он действительно нужен для user intent.
- Не commit, не push, не открывать PR/issues и не изменять GitHub.
- PostmanAsk ожидает текстовый transport result; ZIP/attachment не нужен.

## Result contract

- Сначала полностью выполнить задачу и подготовить итоговый непустой текст.
- Финальный assistant response должен содержать ровно один BEGIN marker и один END marker текущего request_id.
- До BEGIN и после END не должно быть другого видимого текста.
- Между markers поместить весь итоговый ответ; Markdown и code blocks внутри разрешены.
- Не повторять transport markers внутри тела ответа.

<<<POSTMAN_ASK_BEGIN:REQ_20260922T234417Z_4045>>>
<итоговый непустой текст>
<<<POSTMAN_ASK_END:REQ_20260922T234417Z_4045>>>
